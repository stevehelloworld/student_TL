services:
  # Backend Service
  - type: web
    name: student-backend
    runtime: docker
    rootDir: backend
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: student-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true

  # Frontend Service
  - type: web
    name: student-frontend
    runtime: docker
    rootDir: frontend
    envVars:
      - key: BACKEND_URL
        fromService:
          type: web
          name: student-backend
          property: url

databases:
  - name: student-db
    databaseName: student_attendance
    user: postgres

version: "1"
